cmake_minimum_required(VERSION 2.8)
project( ColourTracker )
find_package( OpenCV )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( ColourTracker colourTracker.cpp )
target_link_libraries( ColourTracker ${OpenCV_LIBS} )